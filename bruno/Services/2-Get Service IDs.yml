info:
  name: Get Service IDs
  type: http
  seq: 2

http:
  method: GET
  url: {{baseUrl}}/api/services/ids
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get quick reference of all service IDs and names
