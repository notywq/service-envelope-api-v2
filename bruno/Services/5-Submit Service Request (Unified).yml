info:
  name: Submit Service Request (Unified)
  type: http
  seq: 5

http:
  method: POST
  url: {{baseUrl}}/api/services/submit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "serviceId": "SERV-1-04152026",
        "studentId": 123456,
        "courseCode": "IT101",
        "semester": "2025-1",
        "initiator": "student_portal"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Submit service request with service ID in body
