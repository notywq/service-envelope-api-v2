info:
  name: Get Service Details
  type: http
  seq: 3

http:
  method: GET
  url: {{baseUrl}}/api/services/{{serviceId}}
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get detailed information about a specific service
