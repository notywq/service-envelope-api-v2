info:
  name: List All Requests
  type: http
  seq: 1

http:
  method: GET
  url: {{baseUrl}}/api/requests?limit=20&offset=0
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all service requests with pagination
