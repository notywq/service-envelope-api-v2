info:
  name: Resume Request
  type: http
  seq: 6

http:
  method: POST
  url: {{baseUrl}}/api/requests/{{requestId}}/resume
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Resume processing of a paused request
