info:
  name: List by Type
  type: http
  seq: 3

http:
  method: GET
  url: {{baseUrl}}/api/requests?type=courseEnrollment&limit=20&offset=0
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Filter requests by service type
