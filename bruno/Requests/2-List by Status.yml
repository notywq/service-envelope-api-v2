info:
  name: List by Status
  type: http
  seq: 2

http:
  method: GET
  url: {{baseUrl}}/api/requests?status=completed&limit=20&offset=0
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Filter requests by status
