info:
  name: Get Request Details
  type: http
  seq: 4

http:
  method: GET
  url: {{baseUrl}}/api/requests/{{requestId}}
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get complete details about a specific request
