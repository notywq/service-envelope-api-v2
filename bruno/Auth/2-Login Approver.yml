info:
  name: Login Approver
  type: http
  seq: 2

http:
  method: POST
  url: {{baseUrl}}/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "approver@mapua.edu.ph",
        "password": "approver123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Login as approver user to get JWT token
