info:
  name: Login Requester
  type: http
  seq: 3

http:
  method: POST
  url: {{baseUrl}}/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "requester@mapua.edu.ph",
        "password": "requester123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Login as requester user to get JWT token
