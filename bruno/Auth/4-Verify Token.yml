info:
  name: Verify Token
  type: http
  seq: 4

http:
  method: GET
  url: {{baseUrl}}/api/auth/verify
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verify if token is valid
