info:
  name: Health Check
  type: http
  seq: 5

http:
  method: GET
  url: {{baseUrl}}/health
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verify API server is running and healthy
