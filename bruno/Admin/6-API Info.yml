info:
  name: API Info
  type: http
  seq: 6

http:
  method: GET
  url: {{baseUrl}}/api/info
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get API version and information
